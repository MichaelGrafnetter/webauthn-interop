<UserControl x:Class="DSInternals.Win32.WebAuthn.PasskeyUI.AttestationSigningDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:DSInternals.Win32.WebAuthn.PasskeyUI"
             mc:Ignorable="d"
             prism:ViewModelLocator.AutoWireViewModel="True">
    <prism:Dialog.WindowStyle>
        <Style TargetType="Window" BasedOn="{StaticResource {x:Type Window}}">
            <Setter Property="prism:Dialog.WindowStartupLocation" Value="CenterScreen" />
            <Setter Property="ResizeMode" Value="CanResizeWithGrip" />
            <Setter Property="ShowInTaskbar" Value="False"/>
            <Setter Property="SizeToContent" Value="Height"/>
            <Setter Property="Width" Value="600" />
            <Setter Property="MinWidth" Value="500" />
            <Setter Property="MinHeight" Value="370" />
        </Style>
    </prism:Dialog.WindowStyle>
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Authenticator Preset -->
        <local:AuthenticatorPresetMenu Grid.Row="0" Margin="0,0,0,10" PresetSelectedCommand="{Binding SelectPresetCommand}" />

        <!-- Authenticator Parameters -->
        <GroupBox Header="Authenticator Parameters" Grid.Row="1" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="130"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Label Content="AAGUID:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" />
                <TextBox Grid.Row="0" Grid.Column="1" VerticalAlignment="Center" MaxLength="36" ToolTip="Authenticator Attestation GUID (e.g. 00000000-0000-0000-0000-000000000000)">
                    <TextBox.Text>
                        <Binding Path="AaGuidString" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged">
                            <Binding.ValidationRules>
                                <local:GuidValidationRule ValidatesOnTargetUpdated="True" />
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>

                <Label Content="Algorithm:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" />
                <ComboBox ItemsSource="{Binding Algorithms}" SelectedValue="{Binding SelectedAlgorithm, Mode=TwoWay}" DisplayMemberPath="Value" SelectedValuePath="Key" Grid.Row="1" Grid.Column="1" VerticalAlignment="Center" />

                <Label Content="Signature counter:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" />
                <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="1" VerticalAlignment="Center">
                    <TextBox Text="{Binding SignatureCounter, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="80" VerticalAlignment="Center" />
                    <Grid Width="20" Margin="2,0,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition />
                            <RowDefinition />
                        </Grid.RowDefinitions>
                        <RepeatButton Content="&#x25B2;" Grid.Row="0" Command="{Binding IncrementCounterCommand}" FontSize="6" Padding="0" />
                        <RepeatButton Content="&#x25BC;" Grid.Row="1" Command="{Binding DecrementCounterCommand}" FontSize="6" Padding="0" />
                    </Grid>
                </StackPanel>

                <StackPanel Orientation="Horizontal" Grid.Row="3" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,5,0,0">
                    <CheckBox Content="User present" IsChecked="{Binding UserPresent, Mode=TwoWay}" Margin="0,0,20,0" />
                    <CheckBox Content="User verified" IsChecked="{Binding UserVerified, Mode=TwoWay}" />
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Signing Key -->
        <GroupBox Header="Signing Key" Grid.Row="2" Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <DockPanel Grid.Row="0">
                    <Label Content="Key file:" Width="130" VerticalAlignment="Center" />
                    <Button Command="{Binding BrowseKeyFileCommand}" DockPanel.Dock="Right" VerticalAlignment="Center">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE8E5;" FontFamily="{StaticResource FluentIcons}" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0" />
                            <AccessText Text="_Browse..." VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                    <TextBox IsReadOnly="True" VerticalAlignment="Center" Margin="0,0,5,0">
                        <TextBox.Text>
                            <Binding Path="KeyFilePath" Mode="TwoWay" UpdateSourceTrigger="PropertyChanged">
                                <Binding.ValidationRules>
                                    <local:NotEmptyValidationRule ValidatesOnTargetUpdated="True" />
                                </Binding.ValidationRules>
                            </Binding>
                        </TextBox.Text>
                    </TextBox>
                </DockPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="1" Margin="0,5,0,0">
                    <Button Command="{Binding GenerateKeyPairCommand}" ToolTip="Generate a new key pair for the selected algorithm and save to a PEM file">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE72C;" FontFamily="{StaticResource FluentIcons}" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0" />
                            <AccessText Text="_Generate Key Pair" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </GroupBox>

        <!-- Buttons -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Grid.Row="3" Margin="0,5,0,0">
            <Button Command="{Binding SignCommand}" IsDefault="True">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="&#xED25;" FontFamily="{StaticResource FluentIcons}" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0" />
                    <AccessText Text="_Sign" VerticalAlignment="Center" />
                </StackPanel>
            </Button>
        </StackPanel>
    </Grid>
</UserControl>
